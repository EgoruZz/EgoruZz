name: WakaTime Sync
concurrency:
  group: wakatime-sync-${{ github.ref }}
  cancel-in-progress: false

on:
  schedule:
    - cron: "10 0 * * 1"  # Monday at 00:10 UTC (03:10 MSK)
    - cron: "0 */3 * * *" # Every 3 hours at :00
  workflow_dispatch:
    inputs:
      force-refresh:
        description: 'Force full refresh (ignore cache)'
        required: false
        default: 'false'

jobs:
  update-stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup environment
        run: |
          sudo apt-get update
          sudo apt-get install -y jq bc figlet toilet
          command -v cowsay >/dev/null 2>&1 || sudo apt-get install -y cowsay
          command -v fortune >/dev/null 2>&1 || sudo apt-get install -y fortune-mod

      - name: Fetch and process stats
        id: process-stats
        env:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
        run: |
          # Function for progress bars
          generate_progress_bar() {
            local value=${1:-0}
            local max=${2:-100}
            local size=${3:-20}
            local percentage=$(( value * 100 / max ))
            local progress=$(( percentage * size / 100 ))
            local remainder=$(( size - progress ))
            printf -v bar "%${progress}s" ""
            printf -v remainder "%${remainder}s" ""
            echo "${bar// /▇}${remainder// /░} ${percentage}%"
          }

          # Get current week dates (Monday to Sunday)
          current_week_start=$(date -d "last monday" +'%Y-%m-%d')
          current_week_end=$(date -d "this sunday" +'%Y-%m-%d')
          today=$(date +'%Y-%m-%d')

          # Determine report type
          if [[ "${{ github.event_name }}" == "schedule" ]] && [[ "${{ github.event.schedule }}" == "10 0 * * 1" ]] || 
            [[ "${{ inputs.force-refresh }}" == "true" ]]; then
            CACHE_BUSTER=$(date +%s)
            REPORT_TYPE="🚀 WEEKLY RESET"
            HEADER_STYLE=$(toilet -f term "THIS WEEK STATS" 2>/dev/null || figlet "THIS WEEK STATS")
            DATE_RANGE="$current_week_start → $current_week_end"
            echo "::notice::Full refresh mode activated"
          else
            CACHE_BUSTER="cache"
            REPORT_TYPE="📈 AUTO UPDATE"
            HEADER_STYLE=$(figlet -f mini "Live Stats" 2>/dev/null || echo "THIS WEEK STATS")
            DATE_RANGE="$current_week_start → $today"
          fi

          # Get WakaTime data for current week
          API_URL="https://wakatime.com/api/v1/users/current/summaries?api_key=$WAKATIME_API_KEY&start=$current_week_start&end=$current_week_end&cache_buster=$CACHE_BUSTER"
          echo "::group::Fetching WakaTime data"
          curl -sS --retry 3 --retry-delay 2 "$API_URL" > wakatime.json
          echo "API Response Preview:"
          head -n 50 wakatime.json
          echo "::endgroup::"

          # Process data with proper error handling
          if ! jq -e '.data' wakatime.json >/dev/null 2>&1; then
            echo "::error::Invalid API response format"
            exit 1
          fi

          # Calculate totals from all days
          total_seconds=$(jq -r '.cumulative_total.seconds // 0' wakatime.json)
          daily_avg=$(jq -r '.daily_average.seconds // 0' wakatime.json)
          best_day_seconds=$(jq -r '[.data[].grand_total.total_seconds] | max // 0' wakatime.json)
          productivity=$(echo "scale=1; $best_day_seconds * 100 / ($total_seconds + 1)" | bc)
          week_days=$(jq -r '[.data[] | select(.grand_total.total_seconds > 0)] | length' wakatime.json)
          
          # Generate report
          echo '```text' > stats.md
          echo "$HEADER_STYLE" >> stats.md
          echo "⏳ $REPORT_TYPE » $DATE_RANGE" >> stats.md
          echo "🔄 Last sync: $(date +'%Y-%m-%d %H:%M') (MSK)" >> stats.md
          echo "" >> stats.md
          
          # Clean two-column stats
          echo "┌──────────────────────┬──────────────────────┐" >> stats.md
          echo "│ METRIC               │ VALUE                │" >> stats.md
          echo "├──────────────────────┼──────────────────────┤" >> stats.md
          printf "│ Total Time          │ %20s │\n" "$(jq -r '.cumulative_total.text // "0 secs"' wakatime.json)" >> stats.md
          echo "├──────────────────────┼──────────────────────┤" >> stats.md
          printf "│ Daily Average       │ %20s │\n" "$(jq -r '.daily_average.text // "0 secs"' wakatime.json)" >> stats.md
          echo "├──────────────────────┼──────────────────────┤" >> stats.md
          printf "│ Best Day            │ %20s │\n" "$(printf "%02d:%02d" $(($best_day_seconds/3600)) $(($best_day_seconds%3600/60))" >> stats.md
          echo "├──────────────────────┼──────────────────────┤" >> stats.md
          printf "│ Productivity        │ %20s │\n" "$(generate_progress_bar ${productivity%.*} 100 20)" >> stats.md
          echo "├──────────────────────┼──────────────────────┤" >> stats.md
          printf "│ Active Days         │ %20s │\n" "$week_days/7 days" >> stats.md
          echo "└──────────────────────┴──────────────────────┘" >> stats.md
          echo "" >> stats.md
          
          # Top technologies (from all days combined)
          echo "🔝 TOP 5 TECHNOLOGIES" >> stats.md
          jq -r '[.data[].languages[]? | {name: .name, seconds: .total_seconds}] | group_by(.name) | map({name: .[0].name, seconds: (map(.seconds) | add}) | sort_by(.seconds) | reverse | .[0:5] | .[] | "\(.name)|\(.seconds)"' wakatime.json | 
          while IFS='|' read -r name seconds; do
            text=$(printf "%02d:%02d" $(($seconds/3600)) $(($seconds%3600/60)))
            percent=$((seconds * 100 / ($total_seconds + 1)))
            printf "%-15s %-12s %3d%%\n%s\n\n" "$name:" "$text" "$percent" "$(generate_progress_bar $percent 100 20)"
          done >> stats.md
          
          # Weekly activity
          echo "📅 WEEKLY ACTIVITY" >> stats.md
          echo "┌───────┬────────────────┬──────────────────────┐" >> stats.md
          echo "│ Day   │ Coding Time    │ Activity Progress    │" >> stats.md
          echo "├───────┼────────────────┼──────────────────────┤" >> stats.md
          jq -r '.data[] | "\(.range.date)|\(.grand_total.text)|\(.grand_total.total_seconds)"' wakatime.json | 
          while IFS='|' read -r date text seconds; do
            day=$(date -d "$date" +'%a')
            percent=$((seconds * 100 / ($total_seconds + 1)))
            printf "│ %-5s │ %-14s │ %-20s │\n" "$day" "$text" "$(generate_progress_bar $percent 100 20)"
          done >> stats.md
          echo "└───────┴────────────────┴──────────────────────┘" >> stats.md
          echo "" >> stats.md
          
          # Fun fact
          echo "💡 DEV TIP" >> stats.md
          (fortune -s computers programming science education 2>/dev/null || echo "Stay curious! Keep coding!") >> stats.md
          echo '```' >> stats.md

          # Update README
          sed -i '/<!--START_SECTION:waka-->/,/<!--END_SECTION:waka-->/{
              /<!--START_SECTION:waka-->/r stats.md
              d
          }' README.md
          
          # Set outputs
          echo "stats-generated=true" >> $GITHUB_OUTPUT
          rm -f stats.md wakatime.json

      - name: Commit and push changes
        if: steps.process-stats.outputs.stats-generated == 'true'
        run: |
          if git diff --quiet --exit-code README.md; then
            echo "::notice::No changes detected in README.md"
            exit 0
          fi

          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add README.md
          
          if [[ "${{ github.event_name }}" == "schedule" ]] && [[ "${{ github.event.schedule }}" == "10 0 * * 1" ]] || 
             [[ "${{ inputs.force-refresh }}" == "true" ]]; then
            git commit -m "🚀 Обновление недельной статистики [$(date +'%Y-%m-%d')]" \
                       -m "Начало новой недели | Cache buster: $(date +%s)"
          else
            git commit -m "📈 Автообновление статистики [$(date +'%H:%M MSK')]" \
                       -m "Инкрементальное обновление | Триггер: ${{ github.event_name }}"
          fi
          
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git pull --rebase
          git push origin main
