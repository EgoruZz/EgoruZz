name: WakaTime Sync
concurrency:
  group: wakatime-sync-${{ github.ref }}
  cancel-in-progress: false

on:
  schedule:
    - cron: "10 0 * * 1"  # Monday at 00:10 UTC (03:10 MSK)
    - cron: "0 */3 * * *" # Every 3 hours at :00
  workflow_dispatch:
    inputs:
      force-refresh:
        description: 'Force full refresh (ignore cache)'
        required: false
        default: 'false'

jobs:
  update-stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup environment
        run: |
          sudo apt-get update
          sudo apt-get install -y jq bc figlet toilet boxes
          sudo npm install -g carbon-now-cli || echo "Carbon-now-cli installation failed"
          npx playwright install chromium
          command -v cowsay >/dev/null 2>&1 || sudo apt-get install -y cowsay
          command -v fortune >/dev/null 2>&1 || sudo apt-get install -y fortune-mod
          command -v lolcat >/dev/null 2>&1 || sudo gem install lolcat || echo "Lolcat not available"

      - name: Fetch and process stats
        id: process-stats
        env:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
        run: |
          # Функция для прогресс-баров (без изменений)
          generate_progress_bar() {
            local value=${1:-0}
            local max=${2:-100}
            local size=${3:-20}
            local percentage=$(( value * 100 / max ))
            local progress=$(( percentage * size / 100 ))
            local remainder=$(( size - progress ))
            printf -v bar "%${progress}s" ""
            printf -v remainder "%${remainder}s" ""
            echo "${bar// /▇}${remainder// /░} ${percentage}%"
          }

          # Всегда используем недельный диапазон (понедельник -> сегодня)
          CACHE_BUSTER=$(date +%s)
          REPORT_TYPE="📊 ТЕКУЩАЯ НЕДЕЛЯ"
          DATE_RANGE="$(date -d 'last monday' +'%Y-%m-%d') → $(date +'%Y-%m-%d')"
          
          # Получаем данные за текущую неделю
          API_URL="https://wakatime.com/api/v1/users/current/stats?api_key=$WAKATIME_API_KEY&range=week&cache_buster=$CACHE_BUSTER"
          echo "::group::Загрузка данных WakaTime"
          curl -sS --retry 3 --retry-delay 2 "$API_URL" > wakatime.json
          echo "::endgroup::"

          # Process data
          total_seconds=$(jq -r '.data.total_seconds // 0' wakatime.json)
          best_day_seconds=$(jq -r '.data.best_day.total_seconds // 0' wakatime.json)
          productivity=$(echo "scale=1; $best_day_seconds * 100 / ($total_seconds + 1)" | bc)
          week_days=$(jq -r '.data.days | length' wakatime.json)
          
          # Generate report (без верхней картинки)
          echo '```text' > stats.md
          echo "⏳ $REPORT_TYPE » $DATE_RANGE" >> stats.md
          echo "🔄 Last sync: $(date +'%Y-%m-%d %H:%M') (MSK)" >> stats.md
          echo "" >> stats.md
          
          # Main stats - improved alignment
          echo "╔══════════════════════════════════════════════╗" >> stats.md
          echo "║            📊 CODING ANALYTICS              ║" >> stats.md
          echo "╠══════════════════════╦═══════════════════════╣" >> stats.md
          printf "║ Total Time          ║ %21s ║\n" "$(jq -r '.data.human_readable_total // "0 secs"' wakatime.json)" >> stats.md
          echo "╠══════════════════════╬═══════════════════════╣" >> stats.md
          printf "║ Daily Avg           ║ %21s ║\n" "$(jq -r '.data.human_readable_daily_average // "0 secs"' wakatime.json)" >> stats.md
          echo "╠══════════════════════╬═══════════════════════╣" >> stats.md
          printf "║ Best Day            ║ %21s ║\n" "$(jq -r '(.data.best_day.text // "0 secs") + " (" + (.data.best_day.date // "N/A") + ")"' wakatime.json)" >> stats.md
          echo "╠══════════════════════╬═══════════════════════╣" >> stats.md
          printf "║ Productivity        ║ %21s ║\n" "$(generate_progress_bar ${productivity%.*} 100 20)" >> stats.md
          echo "╠══════════════════════╬═══════════════════════╣" >> stats.md
          printf "║ Active Days         ║ %21s ║\n" "$week_days/7 days" >> stats.md
          echo "╚══════════════════════╩═══════════════════════╝" >> stats.md
          echo "" >> stats.md
          
          # Top technologies section
          echo "🌈 TOP 5 TECHNOLOGIES" >> stats.md
          jq -r '.data.languages[0:5] | .[]? | "\(.name)|\(.text)|\(.percent|floor)"' wakatime.json | 
          while IFS='|' read -r name text percent; do
            # Убираем ANSI-коды и используем Unicode-символы
            echo "▌ $name: $text ($percent%)" >> stats.md
            echo "  $(generate_progress_bar $percent 100 20)" >> stats.md
          done
          echo "" >> stats.md
          
          # Weekly activity with fixed alignment
          echo "📅 WEEKLY ACTIVITY" >> stats.md
          echo "┌───────┬────────────────┬──────────────────────┐" >> stats.md
          echo "│ Day   │ Coding Time    │ Activity Progress    │" >> stats.md
          echo "├───────┼────────────────┼──────────────────────┤" >> stats.md
          jq -r '.data.days | to_entries? | .[]? | "\(.key[:3])|\(.value.text)|\(.value.percent|floor)"' wakatime.json | 
          while IFS='|' read -r day text percent; do
            printf "│ %-5s │ %-14s │ %-20s │\n" "$day" "$text" "$(generate_progress_bar $percent 100 20)"
          done >> stats.md
          echo "└───────┴────────────────┴──────────────────────┘" >> stats.md
          echo "" >> stats.md
          
          # Fun fact with fallback
          echo "💡 DEV TIP" >> stats.md
          cowfile=$(find /usr/share/cowsay/cows/ -type f 2>/dev/null | shuf -n 1 || true)
          (fortune -s computers programming science education 2>/dev/null || echo "Stay curious! Keep coding!") | 
          (if [ -n "${cowfile}" ]; then cowsay -f "${cowfile}"; else cat; fi 2>/dev/null) | 
          (lolcat 2>/dev/null || cat) >> stats.md
          echo '```' >> stats.md

          # Update README
          sed -i '/<!--START_SECTION:waka-->/,/<!--END_SECTION:waka-->/{
              /<!--START_SECTION:waka-->/r stats.md
              d
          }' README.md
          
          # Set outputs
          echo "stats-generated=true" >> $GITHUB_OUTPUT

          # Cleanup
          rm -f stats.md wakatime.json /tmp/stats_for_carbon.md
      
      - name: Generate Carbon image
        if: steps.process-stats.outputs.stats-generated == 'true'
        run: |
          if command -v carbon-now &>/dev/null && [ -f stats.md ]; then
            echo "Generating Carbon image..."
            grep -v '```' stats.md > /tmp/stats_for_carbon.md
            carbon-now /tmp/stats_for_carbon.md -t one-dark -l /tmp/carbon.png -h --headless --target local
            echo "carbon-image-generated=true" >> $GITHUB_OUTPUT
          else
            echo "carbon-image-generated=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload Carbon Image Artifact
        if: steps.process-stats.outputs.carbon-image-generated == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: wakatime-stats
          path: /tmp/carbon.png
          retention-days: 7

      - name: Commit and push changes
        if: steps.process-stats.outputs.stats-generated == 'true'
        run: |
          if git diff --quiet --exit-code README.md; then
            echo "::notice::No changes detected in README.md"
            exit 0
          fi

          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add README.md
          
          if [[ "${{ github.event_name }}" == "schedule" ]] && [[ "${{ github.event.schedule }}" == "10 0 * * 1" ]] || 
             [[ "${{ inputs.force-refresh }}" == "true" ]]; then
            git commit -m "🚀 Обновление недельной статистики [$(date +'%Y-%m-%d')]" \
                       -m "Начало новой недели | Cache buster: $(date +%s)"
          else
            git commit -m "📈 Автообновление статистики [$(date +'%H:%M MSK')]" \
                       -m "Инкрементальное обновление | Триггер: ${{ github.event_name }}"
          fi
          
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git pull --rebase
          git push origin main
