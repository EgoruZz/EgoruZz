name: WakaTime Stats
on:
  schedule:
    - cron: "0 18 * * *"  # Ежедневно в 21:00 МСК
  workflow_dispatch:

jobs:
  update-stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install jq
        run: sudo apt-get install -y jq
      
      - name: Fetch and format stats
        env:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
        run: |
          # Получаем данные
          curl -s "https://wakatime.com/api/v1/users/current/stats/last_7_days?api_key=$WAKATIME_API_KEY&cache_buster=$((RANDOM))" > wakatime.json
          
          # Формируем Markdown с иконками
          echo '```text' > stats.md
          echo '⏳ Статистика кодинга за неделю' >> stats.md
          echo '┌───────────────────────┬──────────────┐' >> stats.md
          echo '│ Общее время           │ '$(jq -r '.data.human_readable_total' wakatime.json)' │' >> stats.md
          echo '├───────────────────────┼──────────────┤' >> stats.md
          echo '│ Среднее в день        │ '$(jq -r '.data.human_readable_daily_average' wakatime.json)' │' >> stats.md
          echo '├───────────────────────┼──────────────┤' >> stats.md
          echo '│ Лучший день           │ '$(jq -r '.data.best_day.text + " (" + .data.best_day.date + ")"' wakatime.json)' │' >> stats.md
          echo '└───────────────────────┴──────────────┘' >> stats.md
          echo '' >> stats.md
          
          echo '🔧 Основные технологии:' >> stats.md
          jq -r '.data.languages[] | select(.percent > 5) | "├─ \(.name): \(.text) (\(.percent|floor)%)"' wakatime.json >> stats.md
          echo '' >> stats.md
          
          echo '🔄 Последнее обновление: '$(date +"%Y-%m-%d %H:%M")' (МСК)' >> stats.md
          echo '```' >> stats.md
          
          # Обновляем README
          sed -i '/<!--START_SECTION:waka-->/,/<!--END_SECTION:waka-->/{
              /<!--START_SECTION:waka-->/r stats.md
              d
          }' README.md
          
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git pull origin main
          git add README.md
          git commit -m "📊 Обновление статистики WakaTime"
          git push origin main
      
      - name: Pull latest changes
        run: git pull --rebase origin main
